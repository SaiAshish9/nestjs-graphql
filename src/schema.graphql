type Message {
  id: Int!
  desc: String!
}

type User {
  id: Int!
  fullName: String!
  birthday: String!
  isActive: Boolean!
}

type Query {
  messages: [Message]!
  users: [User]!
}

type Mutation {
  createMessage(desc: String!): Message!
}
